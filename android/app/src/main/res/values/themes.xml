<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base application theme. The full visual system is defined in Compose
        (ui/theme). This XML theme only provides a dark window background so there
        is no white flash before the first Compose frame, and hides the action bar.
    -->
    <style name="Theme.OxArena" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
