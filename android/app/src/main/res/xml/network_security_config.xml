<?xml version="1.0" encoding="utf-8"?>
<!--
    Production traffic to Render is HTTPS/WSS. Cleartext is permitted only to the
    local development hosts (emulator loopback 10.0.2.2 and localhost) so you can
    run the backend on your machine during development.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
